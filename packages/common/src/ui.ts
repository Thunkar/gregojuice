/**
 * UI-layer exports. Anything that pulls in React or MUI lives here so the
 * non-UI entry point (`@gregojuice/common`) stays importable from Node-side
 * scripts and workers.
 */
export * from "./network";
