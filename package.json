{
  "name": "gregojuice",
  "private": true,
  "packageManager": "yarn@4.5.2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "clean": "turbo run clean",
    "test": "turbo run test",
    "compile:contracts": "cd packages/contracts && aztec compile && bash -c 'for contract in $(ls ./target | grep -v .bak); do aztec codegen ./target/$contract -o ./artifacts; done'"
  },
  "devDependencies": {
    "turbo": "^2",
    "typescript": "~5.7.3"
  }
}
